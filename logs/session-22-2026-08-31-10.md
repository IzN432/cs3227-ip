# Session 22 — 2026-08-31 20:04

## Topic
Improving error handling to cover repeated parameters, duplicate tasks, invalid event intervals, saved-data validation, and atomic file replacement.

## Prompts
- Asked whether command format errors, environment issues, and data validation gaps were already covered; asked to handle the remaining ones.

## Codex Actions
- Rejected repeated parameters (previously the last occurrence silently won).
- Rejected duplicate tasks (comparing type, description, and dates; ignoring completion status).
- Required event end time to be strictly after start time.
- Validated loaded saved data using the same rules; existing files with newly invalid records trigger the recovery prompt.
- Used atomic file replacement (write to temp, then replace) to prevent partial saves; reported a save failure if the rename fails.
- Added a clean console exit on end-of-input.
- Added regression tests; all 185 JUnit tests passed on Java 25; both Checkstyle checks passed.

## Outcome
Ekko rejects repeated parameters, duplicate tasks, and zero-length events in both new commands and loaded data; file saving is atomic to prevent data corruption.
