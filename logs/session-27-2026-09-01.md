# Session 27 — 2026-09-01

## Topic
Implementing the `withdraw` command with tests.

## Prompts
- Asked for the list of commands remaining.
- Asked to implement `withdraw`.
- Asked to commit.

## Actions
- Implemented `withdraw <amount>` in `Marketplace`: parses a positive integer, calls
  `currentUser.deductBalance`, shows error on insufficient funds, confirms on success.
- Added tests in `MarketplaceTest` covering: withdraw with valid amount, withdraw exact balance
  (leaves zero), withdraw insufficient funds (error, balance unchanged), withdraw zero/negative
  (error), withdraw non-integer (error), withdraw missing amount (error).
- Committed implementation and tests.

## Outcome
`withdraw` command is fully implemented and tested; all JUnit tests pass.
