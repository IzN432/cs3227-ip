# Session 26 — 2026-09-01

## Topic
Implementing the `balance` and `topup` commands with tests.

## Prompts
- Asked to implement `topup`.
- Asked to ensure all recently implemented functions are covered by tests.
- Asked to implement `balance`.
- Asked to commit.

## Actions
- Implemented `topup <amount>` in `Marketplace`: parses a positive integer, calls
  `currentUser.addBalance`, confirms with updated balance.
- Implemented `balance` in `Marketplace`: displays the current user's coin balance.
- Added tests in `MarketplaceTest` covering: topup with valid amount, topup twice (accumulates),
  topup zero/negative (error), topup non-integer (error), topup missing amount (error),
  balance shows zero initially, balance after topup shows updated value.
- Committed as two commits: one for the command implementations, one for the tests.

## Outcome
`balance` and `topup` commands are fully implemented and tested; all JUnit tests pass.
