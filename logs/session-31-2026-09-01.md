# Session 31 — 2026-09-01

## Topic
Completing the login scene, adding a registration scene, hashing passwords with SHA-256,
and UI polish across the login/register forms and the marketplace.

## Prompts
- (Continued from session 30 after context exhaustion.) Login scene wiring was incomplete.
- Asked to hash passwords in the User class rather than storing them as plain strings.
- Asked to center the "EKKO / Sign in to continue." heading on the login screen.
- Asked to extend the login form with a separator, a "First time?" label, and a Create account button.
- Asked to center the "First time?" label.
- Asked to add an equivalent "Have an account?" label on the registration screen.
- Asked about a NullPointerException when closing the app on the login screen.
- Asked to move "First time?" below the Create account button (and equivalently on the register screen).
- Asked to commit (three separate commits: find command, password hashing, login/register UI).
- Asked to replace the 🪙 emoji with plaintext as it did not render on Windows.
- Asked to commit the emoji fix.
- Asked to add all un-logged sessions to the logs folder.

## Actions
- Completed `startMarketplace(User user)` rename: removed hardcoded user creation, used the
  passed-in authenticated user and the shared `userStore` field.
- Updated `User` to hash passwords with SHA-256 via `MessageDigest`; the raw password is never
  retained; added `checkPassword(String rawPassword)` and package-visible `hash(String)` helper.
- Removed `getHashedPassword()` from `User`; updated `UserStore.authenticate` to call
  `user.checkPassword(rawPassword)` instead of comparing strings directly.
- Updated `UserTest`: removed `getHashedPassword` assertion; added tests for `checkPassword`
  (correct, wrong, case-sensitive), `hash` determinism, collision resistance, and 64-char hex output.
- Added `showRegisterScene(Stage)` and `createRegisterContent(Stage)` to `Main.java`: username,
  password, confirm-password fields; inline validation (blank username, taken username, blank
  password, mismatched confirm); on success adds user to store and transitions to marketplace;
  "Back to sign in" returns to the login scene.
- Centered the EKKO heading VBox on both auth screens with `heading.setAlignment(Pos.CENTER)`.
- Added "First time?" / "Have an account?" muted centered labels below their respective secondary
  buttons on the login and register screens.
- Added `Separator` between primary and secondary button sections on both screens.
- Fixed `stopSession()` NullPointerException: added an early return when `status == null`,
  which indicates the marketplace scene was never initialised (window closed on auth screen).
- Increased text field horizontal padding from 4 px to 10 px in `gui.css` (all input fields).
- Added CSS for secondary buttons (`#createAccountButton`, `#registerBackButton`): dark background,
  light text, darker hover; added separator styling.
- Replaced `\uD83E\uDE99` (🪙) in `updateBalanceLabel` with plain `" coins"` suffix as the glyph
  does not render on common Windows fonts.
- Created session log files 25–31 in `logs/`.
- Committed in four commits: find command + tests, password hashing + tests,
  login/register scenes + CSS polish, coin emoji → plaintext.

## Outcome
The app opens on a login screen; new users can register; passwords are SHA-256 hashed at rest;
the registration and login scenes are visually consistent; closing on the auth screen no longer
crashes; all 203 JUnit tests pass.
